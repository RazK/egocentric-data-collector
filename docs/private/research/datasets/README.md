# Datasets (internal)

> **Internal only — not part of the public proposal.**

Existing public or licensable egocentric video / IMU datasets, useful
for benchmarking capture quality or bootstrapping model training before
the platform's own data collection comes online.

No entries yet. Log references here the same way as `../vendors/`: name,
URL, one neutral line describing what the dataset is and its license,
once that's been checked.
