# Platforms (internal)

> **Internal only — not part of the public proposal.**

Hardware and SDK platforms relevant to building the capture device
itself: camera modules, IMU/sensor-fusion parts, reference designs, and
vendor SDKs (Android UVC, iOS NCM, etc.) considered during the buy vs.
build evaluation referenced in the public proposal's Phase 1 scope.

No entries yet. Log references here the same way as `../vendors/`: name,
URL, one neutral line — save any comparison or selection reasoning for
`docs/private/buy-vs-build/` and `docs/private/architecture/`.
