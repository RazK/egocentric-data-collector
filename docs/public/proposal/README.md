# Public proposal

The published proposal is served by GitHub Pages from the repository
root (`index.html`, `styles.css`, `assets/`), not from this folder —
changing that would mean reconfiguring GitHub Pages' source, which is
out of scope for this pass.

This folder is reserved for future public-facing proposal artifacts
(e.g. a PDF export, an alternate client-facing page) if that's ever
needed. It's empty for now.
